# DataLens Frontend — Баги

## ИСПРАВЛЕНО

### ~~1. `$state` rune в `.ts` файлах — 500 на всех страницах кроме лендинга~~
**Статус:** ИСПРАВЛЕНО
Файлы переименованы в `.svelte.ts`, все страницы (`/login`, `/register`, `/dashboard`) работают.

### ~~4. Кнопки тарифов ничего не делают~~
**Статус:** ИСПРАВЛЕНО
Кнопки стали `<a>` ссылками: `/register?plan=free`, `/register?plan=starter`, `/register?plan=business`, `/register?plan=pro`.

### ~~5. Год в футере устарел~~
**Статус:** ИСПРАВЛЕНО
Футер теперь показывает `© 2026`.

---

## HIGH — не исправлено

### 2. Favicon 404

**Ошибка:** `[404] GET /favicon.svg`
**Причина:** `app.html` ссылается на `%sveltekit.assets%/favicon.svg`, но файл лежит в `src/lib/assets/favicon.svg`, а не в `static/favicon.svg`
**Фикс:** Переместить `src/lib/assets/favicon.svg` → `static/favicon.svg`

### 3. Кнопка "Смотреть демо" ничего не делает

**Страница:** Лендинг `/`
**Причина:** `<button>` без onclick/обработчика
**Фикс:** Добавить обработчик — скролл к демо-секции (Hero dashboard preview) или навигация на `/dashboard` в mock-режиме

---

## LOW — не исправлено

### 6. Нет мобильного меню (гамбургер)

**Страница:** Лендинг `/`
**Причина:** Навбар не адаптируется для маленьких экранов — нет hamburger-меню, пункты не скрываются
**Фикс:** Добавить responsive hamburger menu для `< 768px`

### 7. Скролл по якорю "Тарифы" неточный

**Страница:** Лендинг `/`
**Описание:** При клике на "Тарифы" в навбаре, скролл останавливается так, что заголовок "Выберите план" находится ниже середины экрана, а сверху видны карточки из секции "Возможности"
**Фикс:** Добавить `scroll-margin-top` для секции `#pricing` с учётом высоты fixed навбара
